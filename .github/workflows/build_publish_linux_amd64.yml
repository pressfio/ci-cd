name: Build and publish Builder (linux/amd64)

on:
  push:
    tags:
      - 'builder/*'

jobs:  
  build-publish:
    uses: ./.github/workflows/build_publish.yml
    with:
      platform: linux/amd64
    secrets: inherit